apiVersion: milvus.io/v1beta1
kind: Milvus
metadata:
  name: my-release
  labels:
    app: milvus
spec:
  dependencies: {}
  components: {}
  config:
    etcd:
      rootPath: my-release
    minio:
      bucketName: my-bucket