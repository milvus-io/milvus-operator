apiVersion: milvus.io/v1beta1
kind: MilvusUpgrade
metadata:
  name: my-release-upgrade
spec:
  milvus:
    namespace: milvus
    name: milvus-sit
  sourceVersion: "2.1.4"
  targetVersion: "2.2.0"
  # operation: upgrade
  # rollbackIfFailed: true
  # bakupPVC: ""
  # deleteUpBackupPVC: true
  deleteBackupPVC: true